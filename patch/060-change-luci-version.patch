--- a/feeds/luci/modules/luci-base/src/mkversion.sh
+++ b/feeds/luci/modules/luci-base/src/mkversion.sh
@@ -20,5 +20,5 @@ else
 end
 
 luciname    = "${3:-LuCI}"
-luciversion = "${2:-Git}"
+luciversion = " Trunk "
 EOF
